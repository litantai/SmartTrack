# 🚀 GitHub Action 工作流触发指南 (Step-by-Step Visual Guide)

本文档提供了如何手动触发 GitHub Actions 工作流来批量创建 Issues 的详细步骤。

---

## 📋 准备工作

在触发工作流之前，请确保：

1. ✅ 工作流文件已合并到主分支（`main` 或 `master`）
2. ✅ `docs/issues/` 目录下有待创建的 Markdown 文件
3. ✅ 你有仓库的写权限（可以创建 Issues）

---

## 🎯 触发步骤（Web UI 方式）

### Step 1: 访问 GitHub 仓库

在浏览器中打开你的仓库：

```
https://github.com/litantai/SmartTrack
```

### Step 2: 进入 Actions 页面

点击顶部导航栏的 **"Actions"** 标签：

```
┌─────────────────────────────────────────────────────┐
│  <> Code   Issues   Pull requests   Actions   ...   │  ← 点击这里
└─────────────────────────────────────────────────────┘
```

或者直接访问：
```
https://github.com/litantai/SmartTrack/actions
```

### Step 3: 选择工作流

在左侧的工作流列表中，找到并点击：

```
┌──────────────────────────────────┐
│  All workflows                   │
│  ┌────────────────────────────┐  │
│  │ 📋 批量创建 GitHub Issues  │  │  ← 点击这个
│  └────────────────────────────┘  │
└──────────────────────────────────┘
```

### Step 4: 运行工作流

1. 在页面右侧，你会看到一个蓝色的下拉按钮：

```
┌─────────────────────────────────────────┐
│  This workflow has a workflow_dispatch │
│  event trigger.                         │
│                                         │
│  ┌───────────────────────────────────┐ │
│  │  Run workflow ▼                   │ │  ← 点击这里
│  └───────────────────────────────────┘ │
└─────────────────────────────────────────┘
```

2. 点击后会弹出一个配置对话框：

```
┌─────────────────────────────────────────────┐
│  Use workflow from                          │
│  ┌─────────────────────────────────────┐   │
│  │ Branch: main               ▼        │   │  ← 选择分支
│  └─────────────────────────────────────┘   │
│                                             │
│  是否跳过已存在的 Issue                     │
│  ┌─────────────────────────────────────┐   │
│  │ true                       ▼        │   │  ← 选择是否跳过
│  └─────────────────────────────────────┘   │
│  (Skip existing issues)                     │
│                                             │
│  ┌───────────────────────────────────────┐ │
│  │  Run workflow                         │ │  ← 点击运行
│  └───────────────────────────────────────┘ │
└─────────────────────────────────────────────┘
```

**参数说明**：
- **Branch**: 选择 `main`（或你的默认分支）
- **是否跳过已存在的 Issue**:
  - `true` (推荐): 如果已存在相同标题的 Issue，则跳过创建
  - `false`: 即使存在同名 Issue 也会重复创建

3. 点击绿色的 **"Run workflow"** 按钮

### Step 5: 查看运行状态

工作流开始运行后，你会看到：

```
┌──────────────────────────────────────────────────────┐
│  批量创建 GitHub Issues (Batch Create GitHub Issues) │
│                                                       │
│  ┌──────────────────────────────────────────────┐   │
│  │ 🟡 feat: Add GitHub Action workflow...        │   │  ← 正在运行
│  │    by username · workflow_dispatch            │   │
│  │    just now · 0s                              │   │
│  └──────────────────────────────────────────────┘   │
└──────────────────────────────────────────────────────┘
```

状态图标说明：
- 🟡 **黄色圆圈**：正在运行
- ✅ **绿色对勾**：运行成功
- ❌ **红色叉号**：运行失败

### Step 6: 查看详细日志

点击运行记录可以查看详细执行日志：

```
┌────────────────────────────────────────────────┐
│  从 Markdown 文件创建 Issues                   │
│  ────────────────────────────────────────────  │
│  ✅ 📥 检出代码 (Checkout Repository)          │
│  🟡 🚀 批量创建 Issues (Create Issues...)      │  ← 展开查看详情
│     📋 列出所有 Issues (List All Issues)      │
└────────────────────────────────────────────────┘
```

点击每个步骤可以展开查看详细输出：

```
📋 开始批量创建 GitHub Issues
======================================

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📄 处理文件: ISSUE_000_INFRASTRUCTURE.md
  📌 标题: Issue #0: 【Infrastructure】详细设计规范文档与 TDD 基础环境搭建
  🏷️  标签: infrastructure,priority:P0,complexity:high,documentation
  🔄 正在创建 Issue...
  ✅ 成功创建 Issue #1

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📄 处理文件: ISSUE_T004_VEHICLE_MODEL.md
  📌 标题: Issue #T004: Vehicle 模型定义
  🏷️  标签: data-layer,priority:P1,complexity:medium,parallel:yes
  🔄 正在创建 Issue...
  ✅ 成功创建 Issue #2

...

======================================
📊 执行统计报告
======================================
  总计处理文件: 4
  ✅ 成功创建: 4
  ⏭️  跳过: 0
  ❌ 失败: 0
======================================
🎉 所有 Issue 创建完成！
```

### Step 7: 验证创建的 Issues

工作流成功运行后，访问 Issues 页面查看创建的 Issues：

```
https://github.com/litantai/SmartTrack/issues
```

你应该能看到新创建的 Issues：

```
┌──────────────────────────────────────────────────────────┐
│  #4  Issue #T006: Booking 模型定义                       │
│      data-layer priority:P1 complexity:high              │
│      opened 1 minute ago by github-actions[bot]          │
├──────────────────────────────────────────────────────────┤
│  #3  Issue #T005: Venue 模型定义                         │
│      data-layer priority:P1 complexity:medium            │
│      opened 1 minute ago by github-actions[bot]          │
├──────────────────────────────────────────────────────────┤
│  #2  Issue #T004: Vehicle 模型定义                       │
│      data-layer priority:P1 complexity:medium            │
│      opened 1 minute ago by github-actions[bot]          │
├──────────────────────────────────────────────────────────┤
│  #1  Issue #0: 【Infrastructure】详细设计规范文档...     │
│      infrastructure priority:P0 complexity:high          │
│      opened 2 minutes ago by github-actions[bot]         │
└──────────────────────────────────────────────────────────┘
```

---

## 🔧 通过 GitHub CLI 触发（可选）

如果你更喜欢使用命令行，可以安装 [GitHub CLI](https://cli.github.com/) 并运行：

```bash
# 认证（首次使用需要）
gh auth login

# 触发工作流
gh workflow run "批量创建 GitHub Issues (Batch Create GitHub Issues)" \
  --ref main \
  -f skip_existing=true

# 查看最新运行状态
gh run list --workflow="批量创建 GitHub Issues (Batch Create GitHub Issues)" --limit 1

# 查看运行日志
gh run view --log
```

---

## ⚠️ 常见问题

### Q1: 找不到 "Run workflow" 按钮

**原因**：
- 工作流文件未合并到主分支
- 工作流文件 YAML 语法错误

**解决方案**：
1. 确保工作流文件在主分支上
2. 检查 Actions 页面是否显示错误信息
3. 验证 YAML 语法是否正确

### Q2: 工作流运行失败

**原因**：
- 权限不足
- `docs/issues/` 目录不存在或为空
- Markdown 文件格式不正确

**解决方案**：
1. 检查仓库设置 → Actions → General → Workflow permissions
2. 确保选择了 "Read and write permissions"
3. 查看详细日志定位具体错误

### Q3: Issues 重复创建

**原因**：
- 运行时选择了 `skip_existing=false`
- Issue 标题发生了变化

**解决方案**：
- 下次运行时选择 `skip_existing=true`
- 手动关闭或删除重复的 Issues

### Q4: 标签未正确提取

**原因**：
- Markdown 文件中标签格式不匹配

**解决方案**：
确保文件中包含以下格式：
```markdown
- **Labels**: `label1`, `label2`, `label3`
```

---

## 📚 相关文档

- **详细使用指南**: `.github/workflows/USAGE_GUIDE.md`
- **工作流文件**: `.github/workflows/init-issues.yml`
- **Issue 模板目录**: `docs/issues/`

---

## 🎉 完成！

现在你已经知道如何手动触发 GitHub Actions 工作流来批量创建 Issues 了！

如有任何问题，请查看：
- 工作流运行日志
- GitHub Actions 文档：https://docs.github.com/en/actions
- 提交 Issue 寻求帮助

---

**最后更新**: 2026-01-27
**创建者**: GitHub Copilot AI Assistant
