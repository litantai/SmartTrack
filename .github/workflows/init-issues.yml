name: 批量创建 GitHub Issues (Batch Create GitHub Issues)

# 🎯 触发方式：仅支持手动触发 (Manual Trigger Only)
on:
  workflow_dispatch:
    inputs:
      skip_existing:
        description: '是否跳过已存在的 Issue (Skip existing issues)'
        required: false
        default: 'true'
        type: choice
        options:
          - 'true'
          - 'false'

# 🔐 权限配置 (Permissions)
permissions:
  issues: write
  contents: read

jobs:
  create-issues:
    name: 从 Markdown 文件创建 Issues
    runs-on: ubuntu-latest

    steps:
      # Step 1: 检出代码仓库
      - name: 📥 检出代码 (Checkout Repository)
        uses: actions/checkout@v4

      # Step 2: 确保所有需要的 Labels 存在
      - name: 🏷️ 检查并创建 Labels (Ensure Labels Exist)
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -e

          # 颜色定义
          GREEN='\033[0;32m'
          BLUE='\033[0;34m'
          YELLOW='\033[1;33m'
          RED='\033[0;31m'
          NC='\033[0m' # No Color

          echo -e "${BLUE}======================================${NC}"
          echo -e "${BLUE}🏷️  检查并创建必需的 Labels${NC}"
          echo -e "${BLUE}======================================${NC}"
          echo ""

          # 定义所有需要的标签及其颜色
          # 格式: "标签名称|颜色代码|描述"
          declare -a REQUIRED_LABELS=(
            "infrastructure|0052CC|Infrastructure and tooling tasks"
            "foundation|1F77B4|Foundation and core setup tasks"
            "data-layer|0E8A16|Data models and database schemas"
            "service-layer|5319E7|Service layer implementation"
            "api-layer|C5DEF5|API routes and endpoints"
            "ui-layer|FBCA04|UI components and pages"
            "business-ui|FFA500|Business-specific UI components"
            "state-machine|9C27B0|XState state machine implementation"
            "rules-engine|FF6F00|Zen Engine business rules"
            "testing|28A745|Testing and test infrastructure"
            "documentation|0075CA|Documentation improvements"
            "priority:P0|B60205|Critical priority - Must be done first"
            "priority:P1|D93F0B|High priority - Important tasks"
            "priority:P2|FF8C00|Medium priority - Should be done soon"
            "priority:P3|FFA500|Low priority - Can be deferred"
            "priority:P4|FFD700|Very low priority - Nice to have"
            "complexity:very-high|8B0000|Very high complexity task"
            "complexity:high|D73A4A|High complexity task"
            "complexity:medium|FBCA04|Medium complexity task"
            "complexity:low|28A745|Low complexity task"
            "parallel:yes|1D76DB|Can be worked on in parallel"
            "parallel:no|6C757D|Cannot be worked on in parallel"
            "auto-created|7057FF|Automatically created issue"
          )

          # 获取仓库中已存在的标签
          existing_labels=$(gh label list --json name --jq '.[].name')

          # 遍历所有需要的标签
          for label_config in "${REQUIRED_LABELS[@]}"; do
            IFS='|' read -r label_name label_color label_description <<< "$label_config"
            
            # 检查标签是否已存在
            if echo "$existing_labels" | grep -q "^${label_name}$"; then
              echo -e "  ✓ 标签已存在: ${GREEN}${label_name}${NC}"
            else
              # 创建标签
              echo -e "  ${YELLOW}+ 创建标签: ${label_name}${NC}"
              if gh label create "$label_name" --color "$label_color" --description "$label_description" 2>&1; then
                echo -e "  ${GREEN}✅ 成功创建标签: ${label_name}${NC}"
              else
                echo -e "  ${RED}❌ 创建标签失败: ${label_name}${NC}"
              fi
            fi
          done

          echo ""
          echo -e "${GREEN}🎉 所有必需的标签已就绪！${NC}"
          echo ""

      # Step 3: 读取 docs/issues/ 目录下的所有 Markdown 文件并创建 Issue
      - name: 🚀 批量创建 Issues (Create Issues from Markdown Files)
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SKIP_EXISTING: ${{ github.event.inputs.skip_existing }}
        run: |
          set -e

          # 颜色定义
          GREEN='\033[0;32m'
          BLUE='\033[0;34m'
          YELLOW='\033[1;33m'
          RED='\033[0;31m'
          NC='\033[0m' # No Color

          echo -e "${BLUE}======================================${NC}"
          echo -e "${BLUE}📋 开始批量创建 GitHub Issues${NC}"
          echo -e "${BLUE}======================================${NC}"
          echo ""

          # 切换到 docs/issues 目录
          cd docs/issues

          # 统计变量
          total_files=0
          created_count=0
          skipped_count=0
          failed_count=0

          # 遍历所有 .md 文件（排除 README.md 和 SUMMARY.md）
          for file in *.md; do
            # 跳过非 issue 文件
            if [[ "$file" == "README.md" || "$file" == "SUMMARY.md" ]]; then
              echo -e "${YELLOW}⏭️  跳过文档文件: $file${NC}"
              continue
            fi

            total_files=$((total_files + 1))

            echo -e "${BLUE}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}"
            echo -e "${BLUE}📄 处理文件: $file${NC}"

            # 从文件内容中提取 Issue 标题（第一行的 # 标题）
            title=$(head -n 1 "$file" | sed 's/^#\s*//')

            if [[ -z "$title" ]]; then
              echo -e "${RED}❌ 错误: 无法从文件 $file 提取标题${NC}"
              failed_count=$((failed_count + 1))
              continue
            fi

            echo -e "  📌 标题: ${GREEN}$title${NC}"

            # 读取文件全部内容作为 Issue body
            body=$(cat "$file")

            # 从文件内容中提取 Labels（查找 Labels: 行）
            labels=$(grep "Labels\*\*:" "$file" | sed 's/.*Labels\*\*: //g' | sed 's/`//g' | tr ',' '\n' | sed 's/^ *//g' | sed 's/ *$//g' | paste -sd, - || echo "")

            if [[ -z "$labels" ]]; then
              labels="auto-created"
              echo -e "  🏷️  标签: ${YELLOW}$labels (默认)${NC}"
            else
              echo -e "  🏷️  标签: ${GREEN}$labels${NC}"
            fi

            # 检查是否已存在相同标题的 Issue
            if [[ "$SKIP_EXISTING" == "true" ]]; then
              existing_issue=$(gh issue list --state all --search "in:title \"$title\"" --json number,title --jq '.[].number' | head -n 1)

              if [[ -n "$existing_issue" ]]; then
                echo -e "  ${YELLOW}⚠️  已存在 Issue #$existing_issue，跳过创建${NC}"
                skipped_count=$((skipped_count + 1))
                continue
              fi
            fi

            # 创建 Issue
            echo -e "  ${BLUE}🔄 正在创建 Issue...${NC}"

            if issue_number=$(gh issue create \
              --title "$title" \
              --body "$body" \
              --label "$labels" 2>&1); then

              # 提取 Issue 编号
              issue_num=$(echo "$issue_number" | grep -oP '/issues/\K\d+$' 2>/dev/null || echo "created")
              echo -e "  ${GREEN}✅ 成功创建 Issue #$issue_num${NC}"
              created_count=$((created_count + 1))
            else
              echo -e "  ${RED}❌ 创建失败: $issue_number${NC}"
              failed_count=$((failed_count + 1))
            fi

            echo ""
          done

          # 输出统计信息
          echo -e "${BLUE}======================================${NC}"
          echo -e "${BLUE}📊 执行统计报告${NC}"
          echo -e "${BLUE}======================================${NC}"
          echo -e "  总计处理文件: ${BLUE}$total_files${NC}"
          echo -e "  ✅ 成功创建: ${GREEN}$created_count${NC}"
          echo -e "  ⏭️  跳过: ${YELLOW}$skipped_count${NC}"
          echo -e "  ❌ 失败: ${RED}$failed_count${NC}"
          echo -e "${BLUE}======================================${NC}"

          # 如果有失败的，退出码设为 1
          if [[ $failed_count -gt 0 ]]; then
            echo -e "${RED}⚠️  存在创建失败的 Issue，请检查日志${NC}"
            exit 1
          fi

          echo -e "${GREEN}🎉 所有 Issue 创建完成！${NC}"

      # Step 4: 输出创建的 Issues 列表
      - name: 📋 列出所有 Issues (List All Issues)
        if: success()
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "最近创建的 Issues:"
          gh issue list --limit 20 --json number,title,labels,createdAt --jq '.[] | "  #\(.number) - \(.title) [\(.labels | map(.name) | join(", "))]"'
